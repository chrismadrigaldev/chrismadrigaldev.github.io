import { MainMenuOptions } from "../../data/mainMenuOptions";
import { useMainMenuButtonSprings } from "../../hooks/springs/useMainMenuButtonSprings";
import { MainMenuButton } from "./MainMenuButton";

export const MainMenuButtons = () => {
    // TODO: Utilize the new animation for the main menu buttons in this file
    /*{ mainMenuButtonsSprings } = useMainMenuButtonSprings();
    return (
        <div>
            {MainMenuOptions.map((item, index) => (
                <MainMenuButton key={item.id}>
                    {item.label}
                </MainMenuButton>
            ))}
        </div>
    );*/
}